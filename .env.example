# Read by `docker compose build` for build-time args (docker-compose.yml).
# Copy to .env and fill in for a production/deployed build.
# For local `pnpm dev`, set apps/web/.env instead — this one only affects
# the Docker image build, since Vite inlines VITE_* vars at build time.

VITE_GA_MEASUREMENT_ID=
