// Pure scoring functions land here in Phase 5 (see DEV_PLAN.md §4, §10).
// Both the web worker and the notification cron import from this package —
// there must never be two implementations of the score.
export {};
