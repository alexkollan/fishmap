# Local dev defaults. Copy to .env and adjust as needed.

# GA4 Measurement ID (Admin -> Data Streams -> your web stream, in
# Google Analytics). Leave empty to disable analytics entirely — the app
# no-ops gracefully with no ID set.
VITE_GA_MEASUREMENT_ID=
