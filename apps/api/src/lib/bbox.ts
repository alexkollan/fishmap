// Same Greek bounding box as tools/coastline-pipeline/build.mjs and
// apps/web/src/map/MapCanvas.tsx's GREECE_BOUNDS — kept as a literal here
// too rather than shared across the workspace boundary, matching how those
// two already duplicate it independently.
export const GREECE_BBOX = { minLon: 19.2, minLat: 34.5, maxLon: 29.7, maxLat: 41.8 };
