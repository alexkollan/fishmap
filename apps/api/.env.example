# Local dev defaults. Copy to .env and adjust as needed.
PORT=3001
WEB_ORIGIN=http://localhost:5173

# Deployment-only secrets (deferred for Phase 1 — see DEV_PLAN.md §8, §7.4).
# Not required for local dev.
# ADMIN_PASSWORD_HASH=
# JWT_SECRET=
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
